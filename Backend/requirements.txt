# Only packages the code actually imports.
# Removed torch, mediapipe, gradio, openai and matplotlib — nothing in this
# backend imports them, and together they added several GB to the image.

flask
flask_cors

# Model: keras ships inside tensorflow
tensorflow
scikit-learn
numpy
h5py

# Video / frames
opencv-python

# Narration
google-genai
python-dotenv

# Used by models/train_lstm.py
pandas
